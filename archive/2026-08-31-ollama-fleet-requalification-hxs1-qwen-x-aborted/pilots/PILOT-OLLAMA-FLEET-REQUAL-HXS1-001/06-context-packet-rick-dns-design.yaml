context_packet:
  goal_id: "2026-08-31-ollama-fleet-requalification-hxs1-qwen-x"
  goal_version: 1
  work_order_id: "WO-REQUAL-HX1-RICK-A3-001"
  session_id: "rick-dns-design-20260831-01"
  role: "producer (OS/DNS: rick)"
  milestone: "M3 — DNS / service-FQDN read-only discovery and design"
  objective: >-
    Establish current DNS/network state read-only and produce a service-FQDN
    design and change plan for Gate A.
  current_state:
    host: "hxs-1 (192.168.50.200); control from hxs-5"
    key_identities: "host FQDN hxs-1.hx.local.arpa (registry) vs live NXDOMAIN (fleet evidence); service FQDN qwen-x.hx.local.arpa unestablished"
    relevant_config: "DNS server 192.168.50.1; registry FQDN column claims resolution that live fleet evidence contradicts"
    units_or_services: "none mutated; discovery only"
  owner_decisions:
    - "Intake §9 recommended service FQDN qwen-x.hx.local.arpa is an OWNER DECISION (D1) — the design targets it but does not assume approval"
  constraints:
    - "READ-ONLY; no DNS/router/host/network mutation"
    - "The registry-vs-live contradiction is recorded, not silently resolved"
  evidence_requirements:
    - "DNS query results (nslookup/dig) for hxs-1.hx.local.arpa and qwen-x.hx.local.arpa against 192.168.50.1"
    - "host /etc/hosts and resolved state; listener reachability read-only"
    - "design: exact records, change owner per lane, verification procedure, rollback"
  consulted_records:
    - "servers/SERVER-REGISTRY.md; servers/2026-08-27-fleet-baseline-wave.md; servers/BLUEPRINT-llm-server.md §8"
  handoff:
    - "Deliverable 06-rick-dns-fqdn-design.md reaches execution_accepted on evidence + deterministic gates; feeds Gate A"
    - "Deliverable goes to Carol for catalog receipt (reusable-platform knowledge — synchronous at Gate B)"
  second_brain_evaluation: "See the governing work order — the packet does not re-decide it"
