context_packet:
  goal_id: "GOAL-OMNIROUTE-L1-SECURE-CORE"
  goal_version: 1
  work_order_id: "WO-L1-TRINITY-INSTALL-001"
  session_id: "trinity-l1-install-20260827-01"
  role: "producer (OmniRoute lifecycle engineer: trinity — first mutating commission)"
  milestone: "L1-M2 — pinned install, OD-13 secrets, four-backend registration"
  objective: >-
    hxs-8 runs OmniRoute v3.8.51 as a native systemd service with the OD-13
    secrets design, all four HX backends registered with identity evidence,
    management authN/authZ configured, agent surfaces verified off, and the
    plaintext-snapshot backup design in place — versioned and reversible.
  current_state:
    host: "hxs-8 (192.168.50.207), Ubuntu 24.04.4, kernel 7.0.0-30, 46 GiB RAM, 437 GB free, Node v24.20.0 at /opt/node-v24.20.0 + /usr/local/bin (rick L1-M1 PASS), Etc/UTC + NTP time.cloudflare.com (fleet pin, one-source ×5)"
    source: "/opt/tkv-local/OmniRoute-release-v3.8.51 — VERIFIED identity (13,098/13,098 content-identical to upstream @42a13fe…; manifest 085fb94b…). READ-ONLY: copy to a work location on hxs-8 and build there; the corpus itself must hash byte-identical before and after"
    backends: "Qwen-X hxs-1:11434 (hx-qwen3.8-27b-64k, 766cd9469fb4…, ctx 65536, ACTIVE), Coder-X hxs-2:11434 (hx-qwen3.6-coderx-64k, ec9ebe08a824…, ctx 65536, candidate until its M8 — usable per owner designation), Meta-X hxs-3:11434 (hx-muse-glimmer-64k, 9dffb015db40…, ctx 65536, ACTIVE), Chat-X hxs-4:11434 LOOPBACK-ONLY (hx-qwen3.5-9b-64k, 5936a390c6c2… — register it, record the posture: not LAN-reachable from hxs-8; parity only if posture allows, else posture-blocked not failed)"
    product_facts: "Next.js 16 standalone build; DATA_DIR-anchored SQLite persistence; proxy.ts authz pipeline (anonymous limited view, management full view); secrets table persists PLAINTEXT by default (the reason OD-13 exists); AES-256-GCM connection-row encryption activates ONLY with STORAGE_ENCRYPTION_KEY; backup encryption write-only; --cloud backup flag dead"
    agent_surfaces: "MCP/A2A/ACP/Conductor/copilot/tunnels/cloud agents/skills/plugins — all disabled/blocked by owner rules and the ledger register; skills execution and background jobs need explicit deployment settings (the not-code-enforced finding)"
  owner_decisions:
    - "OD-12 authorized; OD-04 native no-docker; OD-08 amended all-four; OD-13 secrets rule; OD-07 LAN-only + OmniRoute authN/authZ + no host firewall; OD-09 plaintext snapshots + own encryption; local-model-only everywhere"
  constraints:
    - "Secrets discipline: values generated locally, provisioned via systemd Environment= (root-only 0640 drop-in, hash-recorded), NEVER in any artifact/log/receipt/model context — existence + mechanism only"
    - "The corpus never changes (hash-witness before and after); build on the hxs-8 copy; no Docker/containers/snap; no backend changes; no Layer-2+ routing work; no cloud anything"
    - "Everything versioned and reversible with documented inverses; sanitized command log; helpers deleted at task end"
  evidence_requirements:
    - "Source + build evidence (copy hash == corpus, npm ci/build logs key lines, standalone output present)"
    - "Unit + drop-in hashes; effective environment by NAME (no values); service active + health evidence"
    - "Secrets verification (method only): no plaintext JWT/API secret in the DB secrets table; connection-row fields are ciphertext"
    - "Backend registration records with identity evidence per backend (endpoint, alias, digest, ctx, capability notes); live identity verification from hxs-8 for the three reachable backends"
    - "Exposure evidence (bind, loopback preserved, LAN answer from hxs-5, no firewall); management auth split verified"
    - "Disabled-surface verification (agent surfaces off); backup design; documented inverses; sanitized command log"
  consulted_records:
    - "ledger/08-capability-ledger.md (P4/P5/P8 evidence); 01-rick-l1-node-runtime.md; 04-rick-hxs8-readiness.md; DOC-backend-* records; servers/AGENTS.md (records contract — configuration.md for hxs-8 comes at the L1 gate, not this WO)"
  handoff:
    - "Deliverable 03-trinity-l1-install.md goes to Carol for catalog receipt; handoff OPEN until the receipt is cited in the pilot state log; the L1-M3 gate work order (parity ×4 deep, restart ×2, cold reboot, backup+restore drill, rollback drill, configuration.md, owner sign-off package) follows your install landing"
  second_brain_evaluation: "Per the work order — first traffic plane, first secrets-design deployment"
