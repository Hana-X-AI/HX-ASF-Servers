context_packet:
  goal_id: "GOAL-HX1-QWEN38-001"
  goal_version: 1
  work_order_id: "WO-HX1-JOHN-EXP-001"
  session_id: "john-exp-20260826-01"
  role: "producer (Ollama specialist: john / Esme)"
  milestone: "hxs-1 fleet-scoped endpoint exposure (post-M8 bounded change)"
  objective: >-
    Bind Ollama to all interfaces while preserving loopback, enforce the
    192.168.50.0/24 boundary with ufw, and prove the frozen 64K profile is
    untouched — with full versioned evidence and a clean rollback path.
  current_state:
    host: "hxs-1 (192.168.50.200), Ubuntu 24.04.4 LTS, kernel 7.0.0-30-generic"
    resident_model: "hx-qwen3.8-27b-64k:latest, digest 766cd9469fb47c42890616880772f2647fcfe4656b7550e5cc37ab186cc99d8a, size 20,463,789,012 B, 100% VRAM, ctx 65536, Forever (verified 2026-08-26)"
    endpoint_now: "OLLAMA_HOST=127.0.0.1:11434 (loopback-only; remote refusal proven at M5/M7)"
    units: "ollama.service + ollama-preload.service enabled/active; preload TimeoutStartSec=600, script worst-case ≤538 s (35-esme)"
    firewall: "ufw state to be captured (expected inactive — capture, do not assume)"
    consumers: "fixtures and preload call http://127.0.0.1:11434 — loopback MUST remain functional after the change"
  owner_decisions:
    - "2026-08-26 directive: the entire fleet should have access; the same must be true on hxs-1 — 'if not fix it' (hxs-2 goal D2; hxs-1 goal SC-07a)"
    - "Boundary: 192.168.50.0/24 only for 11434; SSH stays /24; default deny incoming otherwise; no NAT port-forward (owner confirms)"
  constraints:
    - "Only the two authorized mutation classes (bind drop-in + service restart; ufw ruleset + enable); NO reboot, NO model change, NO other services"
    - "Rule order verified before enable: allow 22 from /24 FIRST (never lock out SSH); then 11434 from /24; then default deny incoming"
    - "Secrets never printed/logged/stored; askpass helper reads at execution, deleted at task end"
  evidence_requirements:
    - "Pre/post: drop-in file content + sha256, ufw status numbered, /api/ps identity block"
    - "Reachability matrix after change: 127.0.0.1 (local), hxs-5 → 192.168.50.200:11434 (LAN), SSH still up, preload re-run PASS"
    - "ufw ruleset as applied; journal excerpt (no Xid, no errors); NRestarts accounting"
  consulted_records:
    - "29-esme-m6b-profiles.md, 35-esme-preload-budget.md (identity + unit contracts); 16-esme S01–S03 (prior loopback proof, historical)"
  handoff:
    - "Deliverable 39-esme-hxs1-exposure-change.md goes to Carol for catalog receipt; handoff OPEN until the receipt is cited in the state log"
  second_brain_evaluation: "Per the work order — versioned post-closure change pattern"
