work_order:
  task_id: "WO-HX1-JOHN-M7A-001"
  parent_goal: "GOAL-HX1-QWEN38-001"
  goal_file: "goals/2026-08-24-hx1-ollama-qwen38-27b.md"
  milestone: "M7a — three consecutive cold-reboot recovery cycles at the 64K operating profile (AC-007) + first-boot verifications"
  assigned_agent: "john / Esme (profile-briefed sub-agent)"
  objective: "Prove unattended boot recovery: three consecutive cold reboots of hxs-1, each returning the hx-qwen3.8-27b-64k operating profile to ready state without human intervention, with per-cycle evidence per plan 9.2 — plus the first-boot verifications (kernel transition, driver re-proof, Wi-Fi persistence, boot-path state)."
  owner_authorizations:
    - "All three cold reboots PRE-APPROVED within a 2-hour window starting 2026-08-25T06:29Z (state log row 40; owner taking a break). A FOURTH reboot is NOT authorized"
    - "Soak is DELAYED by the owner — no soak activity in this milestone"
    - "BIOS: owner noted pending personal BIOS optimization — informational only; no BIOS action of any kind"
  authoritative_inputs:
    - "agents/john/profile.md (read first)"
    - "pilots/PILOT-HX1-OLLAMA-QWEN27B-001/plan.md (sections 4.4, 4.5, 9.2)"
    - "pilots/PILOT-HX1-OLLAMA-QWEN27B-001/29-esme-m6b-profiles.md (frozen operating profile)"
    - "pilots/PILOT-HX1-OLLAMA-QWEN27B-001/26-rick-pre-m7-readiness.md (boot-path state, NVRM triggers, Wi-Fi persistence mechanism)"
    - "knowledge/catalog/retrieval-packages/2026-08-25-hxs1-m7-package.md (Carol's M7 bundle)"
    - "/opt/tkv-local/ollama (local on hxs-5)"
  execution_path: "Session on hxs-5; hxs-1 (192.168.50.200) via SSH hxsa@192.168.50.200, askpass pattern per /home/hxsa/opt/local-tkv/agent-zero-docs/ssh-info.md; never print, log, or store the secret; delete helpers at task end; verify hostname/peer IP first. NOTE: each reboot drops your SSH session — reconnect with bounded retry (the 08-17 boot once took ~33 min for carrier, D7-explained maintenance; if SSH is not back in 15 minutes, STOP and escalate to Kimi-K3 with evidence — do not power-cycle or improvise)"
  frozen_baseline: "Resident: hx-qwen3.8-27b-64k digest 766cd946...8cc99d8a, ctx 65536, 100% GPU, Forever; Ollama 0.32.15; preload script asserts hx-qwen3.8-27b-64k; hx1.conf OLLAMA_CONTEXT_LENGTH=65536; sleep targets masked; Wi-Fi rfkill soft-blocked (rfkill1); kernel 7.0.0-28 RUNNING with 7.0.0-30 INSTALLED and DKMS pre-built — the first reboot transitions the kernel"
  per_cycle_procedure: "Per plan 9.2, each cycle: (1) capture pre-reboot service/model/GPU state; (2) reboot via sudo systemctl reboot — the approved procedure; (3) record boot completion and readiness timestamps (SSH return, ollama active, preload unit done, /api/ps resident); (4) verify ollama + ollama-preload enabled/active, NRestarts sane; (5) verify /api/ps shows hx-qwen3.8-27b-64k, digest 766cd946..., context_length 65536, size_vram == size; (6) known-answer warm inference (391-class) — no model-reload delay reported; (7) verify both GPUs (nvidia-smi -L + query), journal scan since boot: zero Xid/OOM, NVRM assertions ONLY at runner lifecycle boundaries (26-rick triggers: any Xid, assertions outside lifecycle, growing leftover-mapping counts = ESCALATE); (8) store evidence under the cycle ID. Cycle 1 ADDITIONALLY: confirm kernel is now 7.0.0-30 (or record if -28 persists), re-prove driver on the running kernel (nvidia-smi full, DKMS status — if nvidia-smi fails or a GPU is missing: STOP, do not attempt driver work, escalate to Kimi-K3 for rick); verify Wi-Fi persistence (rfkill1 still soft-blocked — systemd-rfkill restore, rick's mechanism; record state only, no change); wait-online unit state after boot (residue expected to clear — record only); sleep masks still x4"
  time_box: "Owner window closes 2026-08-25T08:29Z. Three clean cycles fit comfortably; if a cycle fails, ONE bounded diagnostic pass (read-only) then escalate — no repeated reboot loops. If the window would expire with at least one clean cycle completed: stop cleanly, ensure the host is healthy and the model resident, and report partial results honestly — do not rush a sloppy cycle"
  prohibited_actions:
    - "a fourth reboot; any soak or long-idle measurement; any config change (any needed fix escalates FIRST — diagnosis read-only); any driver/kernel/DKMS work; any BIOS action; network/firewall/storage changes; keep_alive:0 as a client pattern (controlled digest-swap unload not expected; escalate if it seems needed); touching the frozen evidence"
    - "exceeding this scope — profile section 13 escalation"
  deliverables (into pilots/PILOT-HX1-OLLAMA-QWEN27B-001/):
    - "30-esme-m7a-reboot-cycles.md — knowledge receipt; per-cycle evidence blocks (timestamps, readiness timeline, unit/model/GPU/log proofs); first-boot verification block (kernel, driver, Wi-Fi, wait-online, masks); readiness timings vs the D5 SLO; command log; profile 11.4 validation summary"
  budgets:
    sessions: 1
    transient_retry: 1
  stop_conditions: "driver or GPU failure post-reboot; preload failure after its bounded retry; any Xid; SSH not returning within 15 minutes; host not reaching the frozen ready state — stop, preserve evidence, escalate to Kimi-K3 (owner is on break; leave the host in the safest available state and document it)"
  rollback_or_containment: "no config changes are authorized, so rollback = the unchanged frozen baseline; if a cycle fails mid-way, containment is a healthy host with the model resident at the 64K profile"
  escalation_target: "Kimi-K3"

terminology_note: "2026-08-25 (review finding): the cycles are OS-initiated reboots (sudo systemctl reboot), not literal power cycles — hxs-1 has no BMC (discovery 2026-08-11) and physical power control is owner-domain, so AC-007 here proves OS reboot recovery. A literal power-cycle test, if ever required, is a separately authorized owner-assisted exercise."
