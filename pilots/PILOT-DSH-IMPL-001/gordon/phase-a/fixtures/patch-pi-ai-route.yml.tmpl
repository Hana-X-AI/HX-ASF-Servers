# Gordon fixture: pi-ai hand-declared route to OmniRoute (openai-completions).
# Oracle: packages/llm/llm-pi-ai/src/index.ts:30-52 (route shape), provider.ts:48-50
# (protocol registry). Rendered at runtime; placeholders are __NAME__ style.
- id: llm-pi-ai
  config:
    providers:
      omni:
        displayName: OmniRoute (HX qualification route)
        apiKeyEnv: __KEY_ENV__
        api: openai-completions
        baseURL: __BASE_URL__
        retryPolicy:
          mode: normal
          maxRetries: __MAX_RETRIES__
        models:
          - id: __MODEL_ID__
            name: __MODEL_ID__
            contextWindow: __CTX__
            maxTokens: 4096

- id: agent-default-model
  config:
    provider: omni
    model: __MODEL_ID__
