context_packet:
  goal_id: "GOAL-HXS3-MUSE-001"
  goal_version: 1
  work_order_id: "WO-HXS3-JOHN-M7-001"
  session_id: "john-m7-20260826-01"
  role: "producer (Ollama specialist: john / Esme)"
  milestone: "M7 — ladder 32K/64K/128K + three-profile freeze on Meta-X"
  objective: >-
    Produce Meta-X's measured context ladder and freeze the three-profile set
    exactly as hxs-1's: aliases hx-muse-glimmer-32k/-64k/-128k with digest
    equality, operating default -64k resident at ctx 65536, preload repointed.
  current_state:
    host: "hxs-3 (192.168.50.202), Ubuntu 24.04.4, kernel 7.0.0-30, driver 580.173.02, Ollama 0.32.15"
    resident: "hx-muse-glimmer:latest (alias digest 472ad84e752d…), 17,839,465,428 B, 100% VRAM (GPU0 10,170 + GPU1 8,908 MiB), ctx 32768 (Ollama VRAM-based default — NOT baked), Forever"
    frozen_artifact: "muse-glimmer:30b, digest de878ce33ad81d060001db1469a02eebe4d86f0ad58cfe52dc062fdcbe4464c1 (dense 27.9B, 52 blocks, max ctx 131,072, CLIP 1.92B, ATEM parser present)"
    vram_headroom: "32.6 GB total across both 5060 Ti; dense weights + projector + KV at 128K is the tightest case in the fleet — measure, never assume"
    endpoint: "LAN-scoped, loopback preserved, no firewall; evidence timestamps on hxs-3 are America/Panama (EST/-05:00) per F-08"
  owner_decisions:
    - "D5 64K operating default after the ladder; profiles 'the same way hxs-1's did' (owner 2026-08-26); D3 muse-glimmer:30b identity frozen; no sampling overrides ever"
  constraints:
    - "Only the ladder + profile work in scope. No quality-suite changes (M5 follows with owner thresholds), no reboots, no firewall, no endpoint changes, no template changes"
    - "Needle probe with the rung's OWN alias at ~95% of rung depth (fixture validity gate on prompt_eval_count); thinking content never persisted in evidence (counts only)"
  evidence_requirements:
    - "Per rung: /api/ps (name, digest, size==size_vram, context_length), per-GPU MiB, KV B/token derivation (two depths), needle JSON (fixture_valid, done_reason, needle_found, pass), 3 known-answer spot checks identical across rungs (thinking ON), link speed under load (Gen3 max vs x8 width — the row-2 caveat), journal excerpt (no Xid/OOM)"
    - "Alias digests: ollama show per alias + manifest equality vs rung Modelfile; repoint pre/post sha256 + diffs; final /api/ps at -64k"
  consulted_records:
    - "22-esme-m6-capacity-ladder.md, 29-esme-m6b-profiles.md (hxs-1 pattern); 07-esme-m4-install.md (Meta-X identity); fixtures sha256sums.txt"
  handoff:
    - "Deliverable 09-esme-m7-ladder-profiles.md goes to Carol for catalog receipt; handoff OPEN until the receipt is cited in the state log"
  second_brain_evaluation: "Per the work order — third validated ladder use; Meta-X context profile"
