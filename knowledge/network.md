# Network: local DNS and FQDN access

Condensed from `/opt/tkv-local/dns-fqdn/hx-local-dns-and-fqdn-access.md`, imported
2026-08-24. The source document remains the full procedure reference.

## Model

`TARGET-STATE`; verified working on 2026-08-13.

- Router `HX-Router` at 192.168.50.1 runs `dnsmasq` as the local resolver.
- Domain: `hx.local.arpa`. Clients must use 192.168.50.1 as DNS for these names.
- Persistent records live in `/jffs/hx-hosts` on the router. The loader
  `/jffs/hx-dns-load.sh` replaces the HX-managed block in `/etc/hosts` and reloads
  dnsmasq. `/etc/hosts` itself is regenerated by stock ASUSWRT and is not persistent.
- Naming: `hxs-N.hx.local.arpa` for fleet servers; `hxs-cp.hx.local.arpa` for the
  control plane. Never `hx-N`.
- Allocation: 192.168.50.200–215 in use (hxs-1 through hxs-15 plus hxs-cp).
  First free address: 192.168.50.216.
  [OPEN CORRECTION 2026-08-29, labeled, append-only: the allocation table
  is stale. Current fleet: hxs-20 at 192.168.50.220, hxs-21 at
  192.168.50.21 (outside the .200–.215 block). hxs-7 (192.168.50.206) is
  DECOMMISSIONED per SERVER-REGISTRY open correction. hxs-cp is replaced
  by hxs-5 (192.168.50.204) per owner advisory 2026-08-27. Active fleet
  count: 16 servers. The .200–.215 range and "first free .216" statement
  are preserved as history.]

## Router reboot behavior

`/jffs` survives a reboot, but automatic execution of the loader after reboot is not
proven on this firmware. After any router restart:

```sh
/jffs/hx-dns-load.sh
nslookup hxs-1.hx.local.arpa 192.168.50.1   # verify
```

## Current state

`DISCOVERED` 2026-08-24, checked from hxs-5:
[OPEN CORRECTION 2026-08-29, labeled, append-only: the "FQDN resolution
is DOWN" snapshot below is STALE. Per state-log rows 20 and 45, the
owner re-ran the DNS loader on 2026-08-28 and 16 FQDNs now resolve via
192.168.50.1 (hxs-1..6, hxs-8..15, hxs-20 (.220), hxs-21 (.21)).
hxs-7 has no record (NXDOMAIN) — consistent with decommission. The
original 2026-08-24 snapshot is preserved below as history.]

- FQDN resolution is DOWN: `nslookup hxs-1.hx.local.arpa 192.168.50.1` returns
  NXDOMAIN after a router restart. Use IP addresses until the loader has been re-run.
- Ping: hxs-6 (192.168.50.205) and hxs-7 (192.168.50.206) do not respond. All other
  fleet addresses and hxs-cp respond.
- Recheck 2026-08-24 06:51 UTC: hxs-6 responds again. hxs-7 is still unreachable
  (ping 100% packet loss, TCP/22 "No route to host").
- Recheck 2026-08-24 06:55 UTC: hxs-7 responds on ping and TCP/22. All 16 addresses
  reachable.
