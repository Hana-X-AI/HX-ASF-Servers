document:
  id: "DOC-tkv-corpus-code-rag-graph"
  title: "/opt/tkv-local/code-rag-graph — Evaluation wrapper (1,204 files)"
  type: "corpus"
  declared_purpose: "Evaluation wrapper: code-graph-rag-main nested snapshot plus HX documents — hx-code-graph-rag-roadmap architecture decision and Claude Code prompt (GPT-5.6-sol, 2026-08-14)."
  source:
    author: "UNRESOLVED — conflicting claims preserved: (a) upstream project; (b) HX consultant evaluation (see declared_purpose). No verifiable upstream URL/commit exists (tree is not a git checkout); escalated to Kimi-K3 (batch 6)."
    provided_by: "Agent Zero"
    origin: "/opt/tkv-local/code-rag-graph"
    section: "§whole-document"
    ingestion_date: "2026-08-25T06:12:00Z"
  owner: "Agent Zero"
  applies_to:
    hosts: []
    fqdns: []
    services: []
    agents: []
    repositories: [tkv-local]
    environments: [fleet]
  version: ""
  status: "historical"
  authority_level: "historical-as-found"
  supersedes: []
  superseded_by: []
  security:
    classification: "internal"
    access_restrictions: "none beyond vault access"
    contains_secret_values: false
  relations:
    - predicate: "references"
      target: "TKV source-snapshot corpus (owner-directed sweep, state log row 32)"
      note: "1,204 files excluding .git internals; deep per-file cataloging on demand via retrieval packages"
  validation:
    validated_at: "2026-08-25T21:02:00Z"
    freshness: "historical"
    review_due: "2026-09-24 (corpus manifest re-hash) or on any corpus update"
  sha256: "48116637371384a190171d5bea4d4bee3a4a917a6269c7eee3bd17e1844dcccb"
  canonical_location: "/opt/tkv-local/code-rag-graph"
notes:
  checksum_method: "sha256 = digest of the sorted relative-path file manifest (1204 files, names only), NOT content hashes; regenerated 2026-08-25; directory canonical_location preserved. Manifest re-hash scheduled 2026-09-24 (run-1 follow-up) stands. .git internals excluded from the manifest and from cataloging. Form alignment batch 7 (F-B6-6, hxinfra-archive reference form): method/scope/count text consolidated wholly into notes.checksum_method; the sha256 field now carries ONLY the bare 64-char digest; digest VALUE unchanged (names-only manifest retained until a future content-sensitive migration)."
  upstream_provenance: "upstream URL/commit: unavailable — tree is not a git checkout (no .git metadata present); unpacked archive snapshot"
  context: "Nested code-graph-rag-main is a duplicate of the top-level code-graph-rag-main tree (diff: only an egg-info build artifact differs); catalog the top-level tree as canonical."
  truth_state: "unverified local snapshot; upstream URL/commit missing — escalated (batch 6, T8)"
  corrections_batch6: "source.author carried conflicting alternatives ('upstream project or HX consultant evaluation') — BOTH claims preserved explicitly as UNRESOLVED (no verifiable upstream URL/commit; tree is not a git checkout); authority_level upstream-reference → historical-as-found per the unverified-local-snapshot convention (governor disposition). source.section and ingestion metadata preserved; checksum, review_due, and declared_purpose untouched. Escalation flagged to Kimi-K3 in the batch-6 receipt."
  corrections_batch7: "F-B6-2 APPLIED (governor disposition 2026-08-25): status adopted -> historical and validation.freshness current -> historical — the record truth state is historical-as-found, unverified local snapshot; adopted/current failed CAT-20 letter (nothing historical-as-found marked current). Alignment per the batch-4 wrapper-record convention (harness-research/docling pattern: status historical + freshness historical). The dual-authorship claims stay UNRESOLVED by design (F-B6-1, governor: CORRECT AS-IS — standing flag). Same run: sha256 field form-aligned to the bare 64-char digest (F-B6-6(b)); digest value unchanged, names-only manifest retained until a future content-sensitive migration."
